<div id="header">
    {{!-- {{object.user}}
    {{object.location}} --}}
    {{#if object.user}}
    <h2>Ahoy there, {{object.user.username}} <br>
        Read more below
    </h2>
    {{!-- <button href="/locations/add" id="add-marker"><button>ADD</button></button> --}}
    <a href="/logout"><button>LOGOUT</button></a>
    {{else}}
    <h2><a href="/user/login">LOGIN</a> or <a href="/user/signup">SIGN UP</a> to add your own listings
    </h2>
    {{/if}}
    <a href="/locations"><button>LOCATIONS</button></a>
    {{!-- <a href="/"><button>MAP</button></a> --}}
</div>


<div class="container">
    {{!-- <div id="locationImage"> --}}
    {{!-- </div> --}}
    {{!-- //M changes: remove all IF's and line 21: <img src="{{imgPath}}" alt=""> --}}
    {{#if object.location.imgPath}}
    <img src="{{imgPath}}" alt="">
    {{/if}}
    {{#if object.location.image}}
    <img src="../{{object.location.image}}" alt=""> {{!-- KEEP THIS LINE --}}
    {{/if}}
    {{!-- END OF //M --}}
    <div class="locationCard-solo">
        <h1> {{object.location.placeName}} </h1>

        <h4>{{object.location.builtData}}</h4>

        <p> {{object.location.description}} </p>

        <div class="location buttons">

            <a href="/"> <button> back to map </button> </a>
            {{#if canEdit}}
            <a href="/locations/edit/{{_id}}" </a> <button> edit
                </button>
                <a href="/locations/{{_id}}/delete" </a> <button> delete </button>
                    {{/if}}
        </div>

    </div>